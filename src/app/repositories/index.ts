import { NGORepository as NGORepo } from './ngo.repository'
import { IncidentRepository as IncidentRepo } from './incident.repository'

export const NGORepository = NGORepo
export const IncidentRepository = IncidentRepo